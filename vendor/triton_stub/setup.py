from setuptools import setup, find_packages

setup(
    name="triton",
    version="99.0.0",
    description="Placeholder Triton package for Jetson builds (no functionality)",
    packages=find_packages(),
    python_requires=">=3.8",
)
